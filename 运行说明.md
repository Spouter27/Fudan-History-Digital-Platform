> 分享版：先在项目根目录执行 pnpm run setup；本文的 cd ./backend、cd ./frontend 均以项目根目录为起点。详见根目录 README.md。

# 校史平台当前运行说明

2026-09-16：在原前端上增加了最小后端，代码均已保存。当前状态以本说明和backend/README.md为准；frontend/docs中的原接入手册保留了第一版规划背景。

## 修改前备份

备份文件：
（原作者本机历史备份，不包含在分享包中）

包含修改前34个项目文件，已通过ZIP完整性和逐文件SHA-256核验，包括原计划书、源码、文档、资源与构建产物。排除了可重装node_modules和Git内部文件。
浏览器localStorage不属于项目文件，旧投稿未包含在ZIP，也未被本次修改删除。

恢复原版：解压到新目录，进入frontend运行pnpm install --frozen-lockfile、pnpm dev。不要覆盖当前新增后端。

## 启动当前版本

需要Node.js 24+和pnpm，开两个终端：

~~~powershell
cd ./backend
pnpm install --frozen-lockfile
pnpm start
~~~

~~~powershell
cd ./frontend
pnpm install --frozen-lockfile
pnpm dev
~~~

访问 http://127.0.0.1:5173/#/login 。
本地开发账号和随机密码见backend/data/dev-accounts.txt，首次启动生成。学生A/B用于投稿与隔离测试，审核员用于审核。

当前frontend/.env.local使用以下配置，新环境需重建：

~~~dotenv
VITE_DATA_MODE=api
VITE_API_BASE_URL=/api
~~~

切回demo并重启前端，可查看和导出此前保存在同一浏览器来源中的投稿；两种模式不自动迁移数据。

## 当前能力

目录与检索API、密码登录与会话、用户数据隔离、审核员权限、数据库持久化、投稿/通过/退回/撤回/删除、匿名审核摘要、前端登录入口、数据库备份、带出处的关键词检索接口。

资料仍为虚构演示，真实学校身份认证、PostgreSQL/pgvector、完整RAG生成与Agent、公网部署尚未实现。具体接口和接入步骤见[后端手册](backend/README.md)。
