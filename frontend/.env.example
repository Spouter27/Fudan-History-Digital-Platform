# demo: browser-local prototype; api: connect to the backend contract in docs.
VITE_DATA_MODE=api
# In api mode prefer a same-origin reverse proxy. Never put secrets in VITE_*.
VITE_API_BASE_URL=/api
