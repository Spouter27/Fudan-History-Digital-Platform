# 前端交付验证记录

验证日期：2026-09-15

## 已通过

- pnpm install：依赖安装完成，pnpm-lock.yaml已生成。
- pnpm build：TypeScript类型检查和Vite生产构建通过。
- pnpm test：8项测试通过。
- 本地浏览器验收：时间轴主题筛选、空结果与清除、详情及关联人物、Escape关闭、中文搜索及高亮、人物选择和缩放重置。
- 投稿链路：提交、刷新持久化、审核通过、共同记忆展示、撤回下架、删除。
- 其他交互：人物拖动、键盘选中、退回原因必填、退回后不公开、JSON导出、HTML标签作为普通文字呈现。
- 窄屏：390px下全部页面、320px下时间轴/人物/投稿页未发现页面横向溢出。
- 已检查桌面时间轴、人物关系页和手机时间轴截图；测试过程未捕获到浏览器运行时异常。

## 未覆盖与未实现

- 没有真实后端，未完成真实认证、用户间隔离、数据库、邮件、服务器安全与公网部署验收。
- 内置资料均为虚构，未完成真实史料考证及授权核验。
- 没有做正式并发或弱网性能验收；构建通过不等于达到计划书的3秒性能指标。
- 无AI、地图、社群和附件上传。
- 当前只验证本地Chromium内核浏览器，仍需在团队目标Safari、Firefox及真实手机上做兼容性验收。

本次浏览器检查使用独立临时浏览器上下文，不读取已有账号会话。内部脚本和截图位于忽略目录.qa，包含本机运行工具路径，不属于可移植的项目测试命令。团队可直接复用pnpm test，并按README的演示路径复测UI。
