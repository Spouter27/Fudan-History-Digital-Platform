# Node24 required. Local-only starter: production mode intentionally refuses startup.
PORT=3000
DB_PATH=./data/platform.sqlite
ALLOWED_ORIGINS=http://127.0.0.1:5173,http://localhost:5173,http://127.0.0.1:4173,http://localhost:4173
# No LLM keys are required; /qa/retrieve returns evidence, not generated answers.
